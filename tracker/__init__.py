"""LeetCode Gym — a local, stdlib-only LeetCode study tracker.

The version lives here (not in packaging/) so the server's /api/about, the
desktop About panel and the PyInstaller spec all read one number.
"""
__version__ = "0.3.0"
